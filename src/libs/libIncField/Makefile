######################################################################
# include system-dependent paths etc.
#######################################################################
include $(HOME)/work/Makefile.Rules

##################################################
##################################################
##################################################
HEADERS = libIncField.h

OBJS = IncFieldData.o		\
       PlaneWave.o
       
##################################################
##################################################
##################################################
DEBUG=-O3
CC=g++
CPPFLAGS+=$(DEBUG)
CPPFLAGS+=$(GOOGLE_INCLUDES)

libIncField.a:	$(HEADERS) $(OBJS)
		ar r $@ $^

clean:		
		rm -f *.o *.a
